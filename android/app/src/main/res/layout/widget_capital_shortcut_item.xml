<?xml version="1.0" encoding="utf-8"?>
<!-- One cell of the capital widget's shortcut GridView (see CapitalShortcutsService). The chip is a
     translucent neutral grey, so the label colour the provider picks for the widget's background
     (WidgetTheme.getTextColor) stays readable on it whatever the background is. -->
<TextView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/shortcut_button"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/widget_chip_neutral"
    android:textColor="#111111"
    android:textDirection="rtl"
    android:textSize="12sp"
    android:fontFamily="sans-serif"
    android:gravity="center"
    android:maxLines="1"
    android:ellipsize="end"
    android:paddingTop="8dp"
    android:paddingBottom="8dp"
    android:paddingStart="4dp"
    android:paddingEnd="4dp" />
